# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project Overview

An application for tracking workouts and diet, built around a **trainer/trainee** relationship: one trainer authors fixed workout and diet plans for each of their trainees, and trainees log their actual sessions/food against those plans. Stage 1 is intentionally minimal: get core logging working end-to-end for a small group of users (~10) before adding scale, polish, or advanced features.

**Do not over-engineer.** Avoid speculative abstractions, premature microservices, multi-tenant complexity, or infrastructure aimed at scale we don't need yet. Favor the simplest Django/React implementation that correctly models the data below. Scalability is a *future* concern — note it in comments/TODOs if relevant, but don't build for it now.

## Tech Stack

- **Backend:** Django (+ Django REST Framework for the API)
- **Frontend:** React (web app)
- **Database:** Postgres preferred if available; SQLite is acceptable for Stage 1 local dev
- **Auth:** Simple session or token auth is sufficient for ~10 users — no need for complex SSO/OAuth infra. Both trainers and trainees have their own accounts.

When scaffolding, prefer Django's conventional project layout (`manage.py`, one `config`/project settings app, feature apps like `workouts`, `nutrition`, `accounts`) and Create React App/Vite defaults for the frontend, unless the repo already has an established structure — check first.

## Users: Trainers and Trainees

- A trainer can have multiple trainees; a trainee belongs to exactly one trainer. This is a simple one-to-many relationship — model it as a FK from trainee to trainer (`Trainee.trainer`), no join table needed.
- Both trainers and trainees log in and use the app themselves. Trainers author plans and review trainee progress/adherence; trainees log their own workout sessions and food intake.
- Workout and diet plans are **fixed and defined by the trainer** — trainees log actuals against them, they don't edit the plan itself.

## Core Features (Stage 1 scope)

### 1. Workout Tracking

- Trainer defines a **workout plan** for a trainee as a **rotation of sessions** (Session 1, Session 2, Session 3, …) — not tied to a calendar day or day of week (unlike the Diet plan's `ReferenceMeal`; see "Resolved Design Decisions"). Each `PlanSession` lists planned exercises (`PlanExercise`) with target sets, a target **rep range** (`target_reps_min`/`target_reps_max`, e.g. 8–12), and a default rest time (e.g. 120s, editable). The plan also has a target weekly frequency (`WorkoutPlan.sessions_per_week`, e.g. "3x/week") for the Progress streak.
- Trainee logs a **workout session** by picking a session — defaults to the next one in the rotation after their last completed session, but any session can be logged out of order (pure UX convenience, not enforced). The app pre-fills the prescribed exercises/sets/rep range as a template to log actuals against.
- Logging granularity is **per set**: for each exercise, log the weight used and the reps actually done in each individual set (e.g. dumbbell chest press, 3x8–12 prescribed → log weight + reps done for set 1, set 2, set 3 separately). Rest time actually taken is also logged per set (defaults to the plan's recommended rest, but editable). Each set can optionally be flagged `is_warmup` (excluded from avg-reps-per-set weight suggestions and the Progress strength score) and given an optional `rpe` (1–10) — both purely optional, never required to save a log. A session itself can carry optional free-text `notes` and an optional `duration_minutes`.
- Weight suggestions, personal-record flags, and the rest timer are computed/handled client-side from already-fetched history — no dedicated backend fields for any of them (see "Backend implementation notes (workout tab...)" below).
- Trainer-authored guidance: both `PlanSession` and `PlanExercise` carry a `notes` field the trainee sees read-only while logging (e.g. a session-wide cue like "focus on tempo this week" or a per-exercise cue like "keep your back straight") — distinct from `WorkoutSession.notes` above, which is the trainee's own reflection on a *logged* session, not trainer guidance on the *planned* one. Editable inline on Workout → Plan for trainers only (`IsTrainerWriteTraineeReadOnly`, same as the rest of plan authoring) — a narrow, notes-only slice of trainer plan-authoring UI, not the full session/exercise editor (still not built).
- Core models: `WorkoutPlan` (trainee FK, `sessions_per_week`) → `PlanSession` (plan FK, label/order, notes) → `PlanExercise` (session FK, exercise, target_sets, target_reps_min/max, default_rest_seconds, notes) → `WorkoutSession` (trainee FK, plan_session FK, date, notes, duration_minutes) → `LoggedExercise` (session FK, plan_exercise FK) → `LoggedSet` (logged_exercise FK, set_number, weight, reps_done, rest_seconds, is_warmup, rpe).

### 2. Diet Tracking

- Trainer defines a **diet plan** for a trainee as a **reference meal plan**: meals (e.g. Breakfast, Lunch, Dinner) each offering one or more named **options** (alternatives a trainee can choose between, e.g. Breakfast → "Smoothie" or "Overnight Oats"), each option containing reference ingredients with reference weights (e.g. "150g chicken breast"). A trainee picks one option per meal when logging.
- A **reference document/database** of ingredients and products (`FoodItem`) storing nutrition info per 100g (calories, macros, micros where available).
  - Stage 1: self-seeded reference table, built from whatever ingredients appear in trainers' reference meal plans.
  - Later stage: integrate a public food/barcode API (e.g. Open Food Facts) as an additional/alternative source for `FoodItem` data — same shape, so this is a swap-in, not a redesign.
- Trainee logs actual food intake **against a specific reference meal item** (e.g. "Breakfast → chicken breast") and enters the **actual weight they measured** — this is expected to differ from the reference weight (they're weighing it themselves, not aiming for exact precision).
- App **calculates**, from the actual weight logged: calories, macronutrients (protein, carbs, fat), and micronutrients (where data is available) — computed from the actual weight, not the reference weight. This gives an easy planned-vs-actual comparison for adherence tracking.
- Core models: `DietPlan` (trainee FK) → `ReferenceMeal` (diet_plan FK, label/order, optional `day_of_week`) → `MealOption` (meal FK, label, order — a named alternative, e.g. "Smoothie"; see "Backend implementation notes (meal options...)" below) → `ReferenceMealItem` (option FK, food_item FK, reference_weight_grams) → `FoodItem` (name, barcode nullable, per-100g nutrition incl. 5 micros, source: seeded/off, `kind`: single/composite, `created_by`/`visibility`/`approval_status` for the sharing model, `serving_unit`/`serving_size_grams` as an entry/display convenience, `macro_filters`/`dietary_tags` (both M2M, fixed vocab — see "Backend implementation notes (food bank filters...)" below) for substitution search — see "Backend implementation notes (food bank sharing...)" below) → `FoodItemComponent` (composite FK → FoodItem, ingredient FK → FoodItem, weight_grams — for multi-ingredient/recipe items) → `FoodLog` (trainee FK, one of `reference_meal_item`/`food_item`/`quick_log_item`, optional `logged_meal` FK grouping it into a structured day/meal-slot log, actual_weight_grams, logged_at, snapshot nutrients incl. 5 micros) → `LoggedMeal` (trainee FK, reference_meal FK, date, source: plan/custom — one per trainee/meal-slot/day; see "Backend implementation notes (meal options...)" below).
- A trainee can also log food that isn't tied to any plan: directly against a `FoodItem` (barcode/manual search) or against their own private `QuickLogItem` shortcut (e.g. "my smoothie") — see `QuickLogItem` below.
- Substitution search: a `FoodItem` can carry `macro_filters` (macro *role* - Lean Protein, Complex Carb, Fat/Oil, etc.) and `dietary_tags` (diet-type compatibility - Vegan, Keto, Gluten-Free, etc.), both fixed-vocabulary M2Ms browsable/filterable in the Food Bank UI. Each item also surfaces computed `alternatives` (other items sharing a macro filter, ranked by closeness in calories) - "what else could I eat instead of this" for when you're out of an ingredient. See "Backend/Frontend implementation notes (food bank filters...)" below.

### 3. Trainer Connection

- Trainee ↔ trainer Q&A: a `QAThread` (trainee-opened, subject + status `open`/`answered`/`archived`) containing a back-and-forth of `QAMessage`s from either party. Either party can post messages; either can change the thread's status; only a trainee can open a new thread.
- Trainer notes: a one-way `TrainerNote` (trainer-authored, trainee-read-only) with a `read`/`read_at` flag the trainee sets via a `mark_read` action — e.g. "remember to focus on form" or "good job this week."

### 4. Exercise Bank

- A shared reference bank of exercises (`Exercise`, writable by any trainer, browsable by all trainees — same access pattern as `FoodItem`): name, description, `equipment` (freeform, e.g. "Barbell"/"Bodyweight"), `primary_muscle_groups`/`secondary_muscle_groups` (both M2M `MuscleGroup`, plain-language names — split so the muscle diagram can show primary-vs-secondary intensity), `difficulty_level` (beginner/intermediate/advanced), optional `image`, optional `video_url` (YouTube only, stored as-is), and `alternatives` (self M2M, non-symmetrical — each exercise curates its own list) for swapping out a movement when equipment is unavailable.
- `PlanExercise` (workout plans) references `Exercise` by FK rather than a freeform name, so plans are built from the bank.
- Browse UI (`Workout → Exercise Bank`, read-only for both roles — see "Frontend implementation notes" below): search + filter by muscle group and difficulty, an expandable card per exercise showing a front/back muscle-highlight diagram and its alternatives.

### 5. Daily Tracker

- `DailyMetric`: one row per trainee per day — weight (+ unit), steps, sleep hours.
- `ActivityLog`: freeform activity type (e.g. "volleyball", "tennis") + duration, multiple per day allowed.
- A read-only dashboard endpoint aggregates `DailyMetric` and `WorkoutSession` dates over a date range for a trainee (or, for a trainer, one of their trainees).

## Conventions

- Keep API endpoints RESTful and resource-oriented (e.g. `/api/workouts/sessions/`, `/api/nutrition/food-items/`, `/api/nutrition/logs/`).
- Nutrient math (scaling per-100g values by consumed weight) belongs in a model method or a small service/util function, not duplicated in views or serializers.
- Write migrations for every model change; don't hand-edit migration files.
- Prefer Django REST Framework serializers + viewsets over hand-rolled views unless there's a good reason not to.
- Keep frontend components small and feature-scoped (`WorkoutLogger`, `FoodLogEntry`, etc.) rather than one large page component.
- No auth/infra complexity beyond what ~10 users need — flag it if a request seems to be scope creep for this stage.

## Build Order (Stage 1)

1. ✅ Scaffold Django project (`config` + `accounts`, `workouts`, `nutrition` apps); custom `User`/role model with trainer/trainee distinction and `Trainee.trainer` FK.
2. ✅ `workouts` models + migrations.
3. ✅ `nutrition` models + migrations, including the nutrient-scaling util (per-100g × actual weight).
4. ✅ Seed script/fixtures for a handful of `FoodItem`s (self-seeded reference table).
5. ✅ DRF serializers + viewsets + URLs for all of the above.
6. ✅ Extend the backend: trainer connection (`connection` app), exercise bank + day-of-week plans (`workouts`), flexible food logging + `QuickLogItem` (`nutrition`), daily tracker + dashboard (`tracker` app). See "Backend implementation notes" above.
7. ✅ Scaffold React (Vite) frontend + auth. TypeScript, Vite dev-server proxy to `localhost:8000` (not `django-cors-headers`), and an auth token in `localStorage` were all confirmed with the user — see "Resolved Design Decisions" and "Frontend implementation notes" below. Role-*aware* routing (different nav/landing per role) is not built yet — both roles currently see the same nav shell; role only gates specific affordances (e.g. who can approve a Food Bank submission, or who can edit a Diet → Log entry).
8. ⏳ **In progress.** Trainer UI: build workout plan (author sessions/exercises/rep ranges), build diet plan (author meals/options/reference ingredients), browse/manage the exercise bank, browse/add/approve Food Bank items, reply to Q&A threads, write trainee notes. Of these, only Food Bank and the Exercise Bank (browse-only, no add/edit UI for either role — it's seeded reference data, not user-submitted like Food Bank) are built so far. A trainer *can* already view (read-only) a trainee's Diet → Plan and Diet → Log pages, since those API endpoints allow trainer reads — but there's no trainer-side UI yet to actually author a diet or workout plan; the one sample diet plan in the dev DB was created via a management command, not through the app (see "Frontend implementation notes" and "Backend implementation notes (meal options...)" below).
9. ✅ Trainee UI: all four Workout sub-tabs are now built — Log (session rotation, one-set-at-a-time entry, weight-suggestion banner, client-computed PR badges, rest timer, exercise-history dual-axis chart), Progress (frequency/rest-days stats, plan-consistency streak, top-3-improving/declining movers, a popup session-history browser, and a collapsible searchable exercise-history card), Plan (read-only, with trainer-editable session/exercise notes), and Exercise Bank — see "Backend/Frontend implementation notes (workout tab...)" below. All four Diet sub-tabs are also built: Plan (browse the trainer's reference meal plan, read-only), Log (log actual meals day-by-day against the plan or a custom off-plan swap, with computed macros and planned-vs-actual coloring), Progress (period-scoped trend chart + day-by-day history, same planned-vs-actual coloring convention), and Food Bank (search/filter by macro role and diet tags, add items, computed alternatives) — see the various "Frontend implementation notes (Diet → ...)" sections below. Not yet built: ad hoc barcode/quick-add food logging outside a meal slot, Q&A + notes inbox, daily tracker entry.
10. Trainee/trainer views: session history, adherence view (planned vs. actual — Diet → Log's day cards already show today's planned-vs-actual % as a first cut, but there's no historical/weekly rollup yet), progress dashboard (weight/steps/sleep/training days over time).
11. End-to-end smoke test with a trainer + a couple of trainees, covering all five nav sections.

### Backend implementation notes (steps 1-5, done)

- Django project lives in `backend/` (not repo root): `backend/manage.py`, `backend/config/` (settings/urls), `backend/accounts/`, `backend/workouts/`, `backend/nutrition/`. Virtualenv at `backend/.venv/` (gitignored) — run tooling via `backend/.venv/bin/python manage.py ...` (activation doesn't persist across separate tool calls in this environment).
- `accounts.User` extends `AbstractUser` with `role` (`trainer`/`trainee`) and a self-referential `trainer` FK (`related_name="trainees"`, `limit_choices_to={"role": "trainer"}`). `AUTH_USER_MODEL = "accounts.User"`.
- DRF installed with `SessionAuthentication` + `TokenAuthentication` (`rest_framework.authtoken`), default permission `IsAuthenticated`. Token endpoint: `POST /api/auth/token/`.
- Full model set from the sections above is implemented as designed, with `PROTECT` on `FoodItem`/`ReferenceMealItem` FKs so historical logs can't be silently cascade-deleted.
- Nutrient scaling util: `nutrition/services.py::scale_nutrients(food_item, weight_grams)` — coerces values through `Decimal(str(...))` (raw floats assigned to `DecimalField`s aren't auto-cast and will blow up multiplication otherwise). Exposed via `FoodItem.nutrients_for_weight()`, `ReferenceMealItem.reference_nutrients()`, `FoodLog.actual_nutrients()`.
- Seed command: `python manage.py seed_food_items` (idempotent, `update_or_create` by name) — seeds ~20 common ingredients in `nutrition/management/commands/seed_food_items.py`.
- API is fully wired under `/api/`: `accounts/`, `workouts/`, `nutrition/` (see each app's `urls.py`/`views.py` for the exact resource list — matches the RESTful convention above).
- Access control (reused across all viewsets, not duplicated per-resource):
  - `accounts/permissions.py`: `IsTrainerWriteTraineeReadOnly` (plan-authoring resources) and `IsTraineeWriteTrainerReadOnly` (session/log resources) — both permit safe methods for either role and restrict writes to the appropriate role.
  - `accounts/mixins.py`: `TraineeScopedQuerysetMixin` — set `trainee_path` per viewset (e.g. `"trainee"`, `"session__trainee"`, `"day__plan__trainee"`) and it scopes the queryset to the trainer's own trainees or the trainee's own records.
  - Every writable FK field on every serializer has its `queryset` restricted in `__init__` to what the requesting user may reference (prevents IDOR — e.g. a trainee can't attach a logged set to someone else's session by guessing its ID). `trainee` on session/log serializers is server-set (`perform_create`), never trusted client input.
- Verified end-to-end with an `APIClient`-based smoke test (trainer builds a full plan → trainee logs a session set-by-set; diet plan → food log with computed nutrients; cross-trainer isolation and IDOR attempts correctly rejected). Not committed as an automated test file — was a throwaway script, cleaned up after.

### Backend implementation notes (trainer connection, exercise bank, diet flexibility, daily tracker — done)

- Two new apps: `connection` (trainer↔trainee Q&A + notes) and `tracker` (daily metrics/activity log/dashboard), both under `/api/connection/` and `/api/tracker/`. Exercise bank (`Exercise`, `MuscleGroup`) extends `workouts`; `QuickLogItem` + `FoodLog` rework extend `nutrition`. Existing model names were kept (`PlanDay`, `ReferenceMeal`, `PlanExercise`, `FoodLog`, ...) rather than renamed to match `DATA_MODELS_AND_ROADMAP.md`'s illustrative names, to minimize churn. (`PlanDay` was later renamed to `PlanSession` once the workout tab's real spec arrived and turned out to need rotation-based scheduling, not day-of-week — see "Backend implementation notes (workout tab...)" below.)
- `PlanExercise.name` (freeform text) was replaced with `PlanExercise.exercise` (FK → `Exercise`, `PROTECT`) — a breaking field change, applied cleanly since the dev DB had zero `PlanExercise` rows at the time.
- Media uploads (`Exercise.image`) need `Pillow` (added to `requirements.txt`) and `MEDIA_URL`/`MEDIA_ROOT` (added to settings, served via `static()` in `urls.py` when `DEBUG=True`).
- `workouts/management/commands/seed_exercises.py` mirrors `seed_food_items` — idempotent, seeds ~20 common exercises + muscle groups + a few beginner-friendly `alternatives`. Media fields are left blank (no fabricated image/video URLs).
- `FoodLog.source` has a model-level default (`plan`) purely so the migration didn't need a one-off default on the NOT-NULL column (table was empty); the API still requires the client to pass it explicitly (`FoodLogSerializer.Meta.extra_kwargs = {"source": {"required": True}}`) so a missing `source` is a 400, not a silent default.
- Re-verified end-to-end with another throwaway `APIClient` smoke script covering all of the above (exercise bank browsing, day-of-week plans, all three `FoodLog` paths, QA thread + reply + status change, trainer note + `mark_read`, daily metric + activity log, dashboard for both roles, and IDOR rejection on each new resource) — cleaned up after, not committed.

### Backend implementation notes (food bank sharing, composite items, measurement units — done)

- `FoodItem` gained: `kind` (`single`/`composite`), `created_by` (FK, null for CSV-seeded rows), `visibility` (`private`/`public`), `approval_status` (`pending`/`approved`/`rejected`) for the sharing/moderation model — see the resolved decision below; and `serving_unit`/`serving_size_grams` as an entry/display convenience only (nutrient fields stay canonically per-100g and are unaffected).
- New `FoodItemComponent` model (`composite` FK → `FoodItem` `CASCADE`, `ingredient` FK → `FoodItem` `PROTECT`, `weight_grams`) for multi-ingredient/"recipe" items. `FoodItem.recompute_from_components()` sums each component's scaled nutrients and re-derives the composite's per-100g fields; called whenever a composite is created or its components are updated.
- `FoodItem.visible_to(user)` classmethod is the single source of truth for who can see what: a user's own items regardless of status, plus everyone's approved-public items; trainers additionally see every public item regardless of approval status, so pending submissions surface for review. Reused by `FoodItemViewSet.get_queryset` and every serializer FK restriction that references `FoodItem` (`FoodLogSerializer`, `ReferenceMealItemSerializer`, composite `components`) to keep the app's IDOR-prevention convention consistent.
- New `nutrition/permissions.py::FoodItemWritePermission`: any authenticated user (trainer or trainee) can browse/create; only the item's creator or a trainer can edit/delete it. This is a different, more granular pattern than the app-wide `IsTrainerWriteTraineeReadOnly`/`IsTraineeWriteTrainerReadOnly` classes, needed because `FoodItem` writes are no longer strictly role-based.
- New `POST /api/nutrition/food-items/{id}/review/` action, trainer-only (any trainer, not just the submitter's own — matches `FoodItem`'s existing shared-reference-data trust model), sets `approval_status` to `approved`/`rejected`.
- `nutrition/services.py::NUTRIENT_FIELDS` extended from 7 to 12 entries (added potassium/calcium/iron/vitamin C/vitamin A); `FoodItem` and `FoodLog` gained matching fields/migrations.
- `seed_food_items` was rewritten to read `ingredient_nutrition_reference.csv` from the repo root (idempotent `update_or_create` by name) instead of the old ~20-item hardcoded list — seeds ~100 rows.
- Verified with throwaway `APIClient` scripts (not committed, per existing convention): visibility scoping across trainer/trainee/other-trainee, ownership-based edit permissions, review-action authorization, and composite nutrient math.

### Backend implementation notes (food bank filters, tags, alternatives — done)

- Two new small lookup models, `MacroFilter` and `DietaryTag` (`name` only, unique, ordered - same shape as `MuscleGroup`), M2M'd onto `FoodItem` as `macro_filters`/`dietary_tags`. Seeded fixed vocab: 8 macro filters (Lean Protein, Fatty Protein, Complex Carb, Fruit, Vegetable, Fat/Oil, Legume, Dairy) and 11 dietary tags (Vegan, Vegetarian, Pescatarian, Gluten-Free, Dairy-Free, Keto, Low-Carb, High-Protein, Paleo, Nut-Free, Soy-Free) via new committed `nutrition/management/commands/seed_food_filters_and_tags.py`.
- No source column for either (same situation `difficulty_level` was later in for exercises), so every existing `FoodItem` is auto-classified by a keyword + macro-threshold heuristic in that command (e.g. fat ≤6g/100g among meat/fish/egg/dairy/legume/plant-protein keyword matches → Lean Protein, >6g → Fatty Protein; wheat/bread/pasta keywords → not Gluten-Free; dairy/whey keywords → not Vegan). Caught and fixed two real bugs while building the heuristic: a comma-inside-parentheses substring split that mangled a couple of macro names, and reviewed classification against real ingredients (chicken breast, cream cheese, avocado, seitan) to confirm it holds up.
- `FoodItemViewSet.get_queryset` gained `?macro_filter=<id>` / `?dietary_tag=<id>` (repeatable) query params for faceted browsing.
- New `GET /api/nutrition/food-items/{id}/alternatives/` action: other visible `FoodItem`s sharing at least one `macro_filter` with this item, ranked by closeness in `calories_per_100g`, top 10 - a live query, not a stored relation (unlike `Exercise.alternatives`, which *is* stored - Food Bank items are searched/paginated so there's no "fetch everything once" list to resolve alternatives against client-side, the way Exercise Bank does).
- Verified via curl: alternatives for "Chicken breast (raw)" (120 kcal/100g) correctly surface other Lean Protein items close in calories (turkey breast, cod, tilapia, legumes); IDOR/visibility scoping unaffected.

### Backend implementation notes (meal options, structured meal logging — done)

- `ReferenceMeal` gained an options layer: new `MealOption` model (`meal` FK → `ReferenceMeal` `CASCADE`, `label`, `order`) sits between `ReferenceMeal` and `ReferenceMealItem` (`ReferenceMealItem.option` FK → `MealOption`, replacing the old direct `meal` FK) — a meal can have several named alternatives (e.g. Breakfast → "Smoothie" / "Overnight Oats" / "Eggs & Toast"), matching how `diet_sample.xlsx` structures a reference meal plan. Applied as a breaking field rename since the table was empty at the time (same pattern as the earlier `PlanExercise.exercise` change).
- `nutrition/services.py` gained `sum_nutrients`/`average_nutrients` (list-of-nutrient-dicts helpers, None-safe like `scale_nutrients`). `MealOption.reference_nutrients()` sums its items; `ReferenceMeal.average_nutrients()` averages across its options (a trainee picks one, not all); `DietPlan.average_daily_nutrients()` sums each meal's average — this is the "planned" target used throughout the frontend (Diet → Plan's top summary, Diet → Log's day-card percentages).
- `DietPlanViewSet` gained a `DietPlanDetailSerializer` used only on `retrieve` (nested meals → options → items, nutrients computed server-side at every level, queryset `prefetch_related`d) — `list`/`create` still use the flat `DietPlanSerializer`. New `MealOptionViewSet` (`/api/nutrition/meal-options/`) follows the same `IsTrainerWriteTraineeReadOnly` + `TraineeScopedQuerysetMixin` pattern as `ReferenceMealViewSet`/`ReferenceMealItemViewSet`.
- New `LoggedMeal` model: a trainee's log of one meal slot (`reference_meal` FK) on one `date`, with `source` (`plan`/`custom`), `unique_together(trainee, reference_meal, date)` — one log per meal slot per day; re-logging the same meal/date replaces it rather than adding a second entry (see the corresponding note under "Resolved Design Decisions"). `FoodLog` gained a nullable `logged_meal` FK so each ingredient of a logged meal is still an ordinary `FoodLog` row, reusing its existing nutrient-snapshot-on-save logic (`_compute_nutrients`) unchanged; ad hoc/barcode/quick logs simply leave it null. `LoggedMeal.total_nutrients()` sums its items via `sum_nutrients`.
- `LoggedMealSerializer` is a nested read/write serializer (`items`, many=True) that overrides `create`/`update` to always upsert via `LoggedMeal.objects.update_or_create(trainee, reference_meal, date)` and then fully replaces `items` — the frontend always POSTs the complete current state for a meal rather than doing granular per-item PATCHes. Validates that a `source=plan` log's items all carry a `reference_meal_item` belonging to the meal being logged (no mixing with raw `food_item`s), and the reverse for `source=custom`. `meal_option_label` is a computed read-only field (derived from `items[0].reference_meal_item.option`) rather than a stored FK, since it's fully implied by which items were logged.
- `GET /api/nutrition/logged-meals/?date=YYYY-MM-DD` filters to one day; omitting `date` returns the trainee's full history. Permission is `IsTraineeWriteTrainerReadOnly` (same as `FoodLogViewSet`) — a trainer can view a trainee's logged meals but not log/edit/delete on their behalf; `DELETE` (used by the frontend's "remove this log" action) is likewise trainee-only.
- New committed management command `nutrition/management/commands/seed_diet_plan_sample.py` (unlike the throwaway smoke-test scripts below, this one *is* committed, mirroring `seed_food_items`/`seed_exercises`) — seeds a full sample `DietPlan` for `trainee1` (as if authored by `trainer1`) transcribed from `diet_sample.xlsx`: Breakfast/Lunch/Afternoon Snack/Dinner meals, each with 2-3 named `MealOption`s (Morning Snack and Evening Snack slots are intentionally left empty, to exercise the "only show a meal card if it has data" frontend rule). Reuses existing seeded `FoodItem`s where the name matches case-insensitively and creates ~11 new ones (Frozen mixed berries, Jocko protein powder, Dry oats, etc.) from the spreadsheet's own per-100g reference values. Idempotent - rerunning (`python manage.py seed_diet_plan_sample`) deletes and recreates the plan's meals.
- Verified against the running dev server with curl (matching the existing convention of throwaway, uncommitted verification — not an automated test file): nested plan retrieval and nutrient math cross-checked against `diet_sample.xlsx`'s own subtotals, plan-mode and custom-mode logging, re-log upsert behavior, date filtering, delete, and trainer read-only/403-on-write enforcement.

### Backend implementation notes (workout tab — data model rework + Exercise Bank — done)

- **Rotation vs. day-of-week, resolved**: the workout tab's real spec turned out to need rotation-based sessions ("do the next session in the rotation," never tied to a calendar day) rather than the fixed-weekly-schedule model `PlanDay` was originally built for. `PlanDay` → `PlanSession` (breaking rename, applied cleanly since the table was empty — same pattern as the `MealOption`/`PlanExercise.exercise` renames): dropped `day_of_week` entirely, kept `label`/`order` (order now *is* the rotation sequence). `PlanExercise.day` → `.session`; `WorkoutSession.plan_day` → `.plan_session`. The Diet app's `ReferenceMeal`/`day_of_week` convention is unchanged — the two plan types now genuinely diverge, which is why they no longer share a `day_of_week` field (see "Resolved Design Decisions").
- `PlanExercise.target_reps` (single int) → `target_reps_min`/`target_reps_max` (defaults 8/12) for a rep range. `WorkoutPlan.sessions_per_week` (default 3), `WorkoutSession.notes`/`duration_minutes`, and `LoggedSet.is_warmup`/`rpe` (1–10, validated) were all added the same pass — all empty-table migrations, no backfill needed.
- `Exercise.muscle_groups` (single M2M) → `primary_muscle_groups`/`secondary_muscle_groups` (both M2M `MuscleGroup`) so the muscle diagram can show primary-vs-secondary intensity; added `equipment` (freeform `CharField`). `Exercise.alternatives` (self M2M) was changed to `symmetrical=False` — it was already on the model from the original 20-exercise seed, but symmetrical mode meant `.set()`-ing exercise A's alternatives silently added A to every one of *those* exercises' own alternative lists too, snowballing a curated top-4 into 15+ entries for anything in a large category (caught via a `Barbell Back Squat` spot-check during seeding). No migration needed for this specific change — `symmetrical` is ORM-level behavior, not a schema difference.
- PR detection and the rest timer are intentionally **not** backend fields — PRs are computed client-side by diffing a just-logged set against the exercise's already-fetched history (reusing the same fetch the weight-suggestion feature needs), and the rest timer is a local countdown seeded from `PlanExercise.default_rest_seconds`. Both are Phase B (Log) concerns; nothing to build here yet beyond leaving room for them.
- `WorkoutSessionViewSet`/`LoggedSetViewSet` gained `start`/`end` range-filter query params, same pattern as `LoggedMealViewSet` — added now even though nothing in the Exercise Bank uses them yet, since both the Log's exercise-history chart and Progress's frequency/streak stats (not built) will need them, and it's a one-line addition alongside the other view edits.
- `workouts/management/commands/seed_exercises.py` was rewritten to read all ~100 rows from `workout_sample.xlsx` (added `openpyxl` to `requirements.txt`) instead of the old ~20-item hardcoded list — mirrors `seed_food_items`'s CSV-reading pattern. `difficulty_level` isn't in the source sheet (same situation `seed_food_items`' macro filters were in) so it's hand-classified per exercise name (e.g. barbell squat/deadlift variants and the Olympic lifts are `advanced`; the spec's own example — barbell back squat ≠ beginner — is the check). `alternatives` are auto-populated at seed time (same category + most shared primary muscle groups, capped at 4), not hand-curated per exercise like the original small seed's `ALTERNATIVES` dict. The command self-cleans: any previously-seeded `Exercise` no longer in the current sheet is deleted on rerun, so swapping in an updated sheet doesn't accumulate stale near-duplicate rows (this caught 5 real duplicates — e.g. old "Dumbbell Chest Press" vs. the sheet's "Dumbbell Bench Press" — the first time the rewritten command ran against the old seed's leftovers).
- Verified via curl against the running dev server (uncommitted, throwaway — same convention as elsewhere): full `WorkoutPlan → PlanSession → PlanExercise` write path as `trainer1`, all renamed endpoints (`plan-sessions`, etc.) return 200, and spot-checked seeded exercises' difficulty/muscle-groups/alternatives for sanity.

### Frontend implementation notes (React app scaffold + Diet → Food Bank — done so far)

- Scaffolded in `frontend/` (Vite + React + TypeScript, `react-router-dom` for routing). Design system is Tailwind CSS v4 + shadcn/ui (`shadcn` CLI, Radix base, "Nova" preset — config in `frontend/components.json`); primitives live in `src/components/ui/` (Button, Input, Label, Dialog, Sheet, Card, Badge, Select, ToggleGroup, Separator, Tabs, Textarea). `@/*` path alias → `src/*` (`tsconfig.app.json` + `vite.config.ts`); `cn()` helper in `src/lib/utils.ts`. Brand purple `#4c236b` is wired in as the `--primary` CSS token in `src/index.css`, with a lighter tint under `prefers-color-scheme: dark`.
- Dev server proxies `/api` → `localhost:8000` (`vite.config.ts`) — avoids CORS entirely, no `django-cors-headers` needed.
- Auth: `src/auth/AuthContext.tsx` (token in `localStorage`, `POST /api/auth/token/` + `GET /api/accounts/me/`), `RequireAuth` route guard, `/login` page. **Two dev-only users exist directly in the local (gitignored) `db.sqlite3`** — not a fixture or management command — `trainer1`/`devpass123` and `trainee1`/`devpass123` (trainee1's trainer = trainer1); recreate manually if the DB is ever reset.
- App shell (`src/layout/`): sticky `Header` (brand + hamburger), hamburger opens a `Sheet`-based `NavDrawer` (username/role, disabled Profile/Settings placeholders, working Log out), fixed 5-tab `BottomNav` (Diet/Workout/Progress/Daily/Trainer, `lucide-react` icons) — always visible per the user's requirement, regardless of which tab/section is open.
- Diet tab (`src/pages/diet/`): `DietLayout` renders a floating pill-style segmented sub-nav (Log/Progress/Plan/Food Bank), sticky just below the header with a visible gap ("toggle" look, per the user's request). Built as plain styled `NavLink`s rather than Radix `Tabs`/`ToggleGroup` — those primitives fight router-driven active state, so only their Tailwind classes were reused for the visual style. Only `/diet/food-bank` is implemented; `log`, `progress`, `plan` are placeholder headings.
- Food Bank (`FoodBankPage`, `FoodItemCard`, `AddFoodItemDialog`, `IngredientPicker`): debounced server-side search; expandable cards showing badges (Recipe/Private/Pending approval/Rejected) and, for trainers, inline Approve/Reject on pending public items; a centered `Dialog` for adding items with a Type toggle (single item vs. multiple ingredients), a Measurement selector (unit + grams-per-unit, `src/lib/servingUnits.ts` — see resolved decision below), and — trainee-only — a Visibility toggle. `IngredientPicker` searches the visible food bank to build a composite and has an inline "can't find it, add new ingredient" mini-form; that mini-form is grams/100g-only and intentionally doesn't get the Measurement picker (kept out of scope to limit this round's surface area).
- Role-differentiated navigation is enforced field-by-field rather than via separate nav shells (both roles see the same shell; e.g. Food Bank's Approve/Reject and Visibility controls, or Diet → Log's Edit/remove/"Log this meal" controls, are simply hidden/403'd for the wrong role). Not yet built: Daily Tracker/Trainer nav sections (placeholder pages only), the Measurement picker in `IngredientPicker`'s inline add-new-ingredient form. Diet's Log/Plan/Progress sub-tabs and filters/alternatives on this Food Bank page are now built — see below.

### Frontend implementation notes (Diet → Food Bank filters + alternatives — done)

- `MultiSelectDropdown` (`src/components/MultiSelectDropdown.tsx`) - a reusable checkbox-list dropdown built on Radix `DropdownMenu` (`DropdownMenuCheckboxItem`s, `onSelect={(e) => e.preventDefault()}` so picking one doesn't close the menu) - was built here for the Food Bank's Category/Tags filter row (a 2-up row of dropdowns, replacing an earlier horizontally-scrolling toggle-chip row that read as "hovering through all of them" on mobile) and later reused as-is for the Exercise Bank's Muscle/Difficulty filters. Its optional `searchable` prop (a search `Input` pinned to the top of the menu, `onKeyDown={(e) => e.stopPropagation()}` so Radix's menu-navigation keys don't steal keystrokes from the input) was added for the Muscle filter (28 options) and later reused again for Workout Progress's exercise-history search - **the underlying lesson that keeps recurring**: a dropdown/search-results list living inside a card needs to be a portaled Radix `DropdownMenu`/`Popover`, not a plain absolutely-positioned `<div>`, because cards use `overflow-hidden` for their rounded corners and will silently clip anything absolutely-positioned that grows past their edge.
- `FoodItemNutritionFacts.tsx` was extracted from `FoodItemCard`'s expanded-view rendering (kcal/macro row, macro-filter/dietary-tag badges, micros grid) so it could be reused, unmodified, by the new `FoodItemDetailDialog.tsx` - a popup opened via a card's "See alternatives" button, showing the item's own facts plus a live-fetched alternatives list; clicking an alternative swaps the dialog's content to *that* item (and its own alternatives) in place, so a trainee can hop from substitute to substitute without closing anything.

### Frontend implementation notes (Diet → Plan and Diet → Log — done)

- **Diet → Plan** (`ReferencePlanPage.tsx`, read-only): fetches the trainee's own diet plan (`GET .../diet-plans/` then `.../diet-plans/{id}/` for the nested detail) and renders a `DayAverageSummary` card (the day's planned kcal/macros) above a list of `PlanMealCard`s — one collapsible card per meal *that actually has data* (meals are conceptually reserved in a fixed 6-slot order — Breakfast/Morning Snack/Lunch/Afternoon Snack/Dinner/Evening Snack — but only populated ones render, per the user's requirement). Expanding a meal reveals its `PlanOptionRow`s (kcal/macros per option, auto-lettered "Option A) <label>" from array position so a trainer only has to name the option, not retype the "Option A" prefix); expanding an option reveals its ingredients, which are themselves clickable and open `IngredientNutrientDialog` — a measurement-unit-aware nutrient lookup (grams/oz/lb always available, plus the ingredient's own defined unit like "each"/"serving" when it has one) that fetches the full `FoodItem` and recomputes calories/macros/micros live as the amount/unit changes.
- **Diet → Log** (`LogPage.tsx` + `LogDayCard.tsx` + `LogMealSlot.tsx`, read/write for trainees, read-only for trainers): a scrollable list of day cards, today expanded by default (fetches that day's `LoggedMeal`s immediately on mount), a "Load earlier days" button appends more collapsed day cards (pure client-side date math, no fetch until a card is expanded), and an "Only show today" button resets back to just today. An expanded day card's header shows actual/planned kcal and a percentage, colored green at 90-110% of the plan's `average_daily_nutrients` and reddish otherwise (`lib/dietStatus.ts`); the protein row is reddish only under 90% of target, carbs/fat are never colored (all per the user's explicit thresholds). Below that, one `LogMealSlot` per meal slot in the plan (same "only if populated" rule as the Plan page).
- Each `LogMealSlot` is either "Not logged yet", or a compact logged summary (kcal + "From plan · <option label>", or an "Off plan" destructive-styled badge for a custom log) with **Edit** and a trash-icon **remove** button (`DELETE .../logged-meals/{id}/`, confirmed via `window.confirm`) — both trainee-only (`useAuth()`'s `role` gates them entirely; a trainer sees the same page fully read-only, since only a trainee may write their own log — this was a real bug caught mid-build: the buttons rendered for both roles at first, and a trainer clicking them hit a silent 403). Editing toggles between **From plan** (pick option A/B/C, ingredients + weights pre-filled from that option and freely editable, live-rescaled via `lib/nutrients.ts`'s `scaleNutrients`) and **Custom (off plan)** (`CustomMealItemPicker.tsx`: search the Food Bank, add as many ingredients as needed each with its own amount + measurement-unit picker via `lib/servingUnits.ts`'s `gramsForQuantity`/`gramsPerUnit`, or open the *actual* `AddFoodItemDialog` from the Food Bank tab via "Can't find it? Add a new food item" if it doesn't exist yet). Saving always POSTs the complete current item list (the backend upserts, see above); clearing every ingredient in Custom mode and hitting Save deletes the log instead of erroring — "nothing left to save" is treated as "remove the log," not a validation failure.
- `lib/nutrients.ts` (`nutrientsForWeight`, `scaleNutrients`, `sumNutrients`) and `lib/servingUnits.ts` (`gramsPerUnit`, `gramsForQuantity`, alongside the pre-existing `FIXED_GRAMS_PER_UNIT`/`SERVING_UNIT_OPTIONS`) are shared client-side nutrient-math/unit-conversion utilities now used by both the Plan and Log pages (and `IngredientNutrientDialog`) — they mirror the backend's `scale_nutrients`/`sum_nutrients`/`average_nutrients` so the frontend can live-preview "what if I change this amount" edits without a round trip.

### Frontend implementation notes (Diet → Progress — done)

- `DietProgressPage.tsx` established the period-selector pattern reused everywhere else in the app since (Workout Progress, Session History): a 2×2 grid (top-left/top-right/bottom-left/bottom-right, per the user's explicit request) - This Week / This Month / All Time / Custom, the last revealing From/To date inputs. Re-fetches `listLoggedMealsRange` per period rather than pulling all history at once (unlike Workout Progress, which does fetch everything up front - Diet logs are denser per day, so this keeps payloads smaller).
- `ProgressTrendChart.tsx` - the one chart in the app built with the `dataviz` skill loaded first. Calories and macros (protein/carbs/fat) can't share a linear axis (kcal is in the hundreds, grams in the tens) without a dual-axis chart, which the skill treats as a hard anti-pattern, so the chart is instead **indexed to % of daily target** per nutrient - one shared 0-150%-ish axis, a dashed reference line at 100%, four toggleable series in the validated categorical palette. A hover/touch crosshair drives `ProgressSummaryCard` to swap from "period average" to "that day's actuals" in place, so the same headline numbers serve as both the default view and the tooltip - no separate floating tooltip needed. (Contrast the Workout tab's `ExerciseHistoryChart`, which *is* genuinely dual-axis - weight and reps/set have no shared base to index to, and that's a deliberate, documented exception - see "Frontend implementation notes (workout tab → Log)" below.)
- The 100% reference line is tinted with a reserved "status good" green (`--status-good`, new CSS token, distinct from the four categorical series colors so it never gets mistaken for a series) and labeled "Target" directly on the line, so it reads as the threshold at a glance rather than just another gridline.
- Switching periods used to fully unmount the page to a "Loading…" state and back, which read as a jarring flash. Fixed by only showing that full-page state on the very first load; a period switch now just dims the existing chart/cards to 40% opacity while the new data comes in, keeping layout stable (mirrors the "refetch keeps the frame" rule from the `dataviz` skill's interaction guidance, applied even though this isn't strictly a chart-only fix).
- `ProgressDayCard.tsx` is a read-only history view (via a bulk `start`/`end`-ranged fetch, grouped client-side by date) - expanding a day shows only what was actually logged that meal slot (the chosen option label, or "Off plan" for a custom swap, plus each ingredient's logged weight) not the full set of plan options, unlike the Plan tab.

### Frontend implementation notes (Workout → Exercise Bank — done)

- `WorkoutLayout` (`src/pages/workout/`) mirrors `DietLayout` exactly — same sticky pill sub-nav, same 4-tab shape (Log/Progress/Plan/Exercise Bank), replacing the old flat `WorkoutPage.tsx` placeholder. `/workout` now nests routes under it the same way `/diet` does; `log`/`progress`/`plan` are still placeholder headings, only `exercise-bank` has real content.
- `ExerciseBankPage` mirrors `FoodBankPage`, with one simplification: since the exercise bank is only ~100 rows (vs. Food Bank's open-ended, user-submitted set) and read-only for both roles (no add/edit UI — it's seeded reference data, not user-submitted), it fetches the *entire* list once on mount and does search/filter entirely client-side, rather than a debounced server-side search per keystroke. Reuses `MultiSelectDropdown` (built for Food Bank's Category/Tags filters) as-is for the Muscle-group and Difficulty filters.
- `ExerciseCard` mirrors `FoodItemCard`'s collapsed/expanded pattern (name + difficulty + primary-muscle badges collapsed; description/equipment/secondary muscles/diagram/alternatives expanded). Alternatives are resolved client-side from the already-in-memory exercise list via the `alternatives` id array — no dialog or second fetch needed, unlike Food Bank's `FoodItemDetailDialog` (which needs one because Food Bank items are searched/paginated, not fetched in full).
- `MuscleDiagram` wraps the `body-highlighter` npm package (the maintained, framework-agnostic fork — *not* the original `react-body-highlighter`, which has an actual `<Model>` React component but a different, less current API). Being framework-agnostic and dependency-free, it has no React peer-dependency at all, so React-19 compatibility was a non-issue in practice despite being flagged as a risk beforehand. It's imperative (`createBodyHighlighter({container, ...})` mounts straight to a DOM node, returns `{update, destroy}`), so `MuscleDiagram` wraps it in the standard `useRef` + `useEffect(() => { ...; return () => instance.destroy() }, [deps])` pattern rather than a declarative component. Renders two instances side by side (`type="anterior"` and `type="posterior"`) since one exercise's muscles can span both views. Primary-vs-secondary intensity uses the library's own `frequency` field directly (`{ muscles, frequency: 2 }` for primary, `frequency: 1` for secondary, paired with a 2-color `highlightedColors` ramp) rather than any workaround. The library's fixed muscle-slug vocabulary doesn't cover our freeform `MuscleGroup` names (confirmed by testing its own `normalizeMuscle` alias resolver directly — it only catches near-exact singular/plural spelling, not synonyms like "Traps"→trapezius or "Glutes"→gluteal), so `MuscleDiagram.tsx` hand-maps all 28 seeded muscle group names to the library's slugs; a handful of intentionally-vague ones (Arms, Back, Full Body, Hip Flexors, Legs — only used by the 5 Cardio-category exercises) are left unmapped and simply don't highlight anything.
- Workout → Plan trainer-authoring UI is not built (same situation as Diet → Plan's trainer-authoring side) — the sample plan in the dev DB was created via a management command, not through the app.

### Frontend implementation notes (workout tab → Progress — done)

- No new backend endpoints - Progress fetches the trainee's full `WorkoutSession` history once (`listWorkoutSessions()`, unbounded) and the plan detail, then everything below is pure in-memory recomputation on the client; switching the period selector re-filters instantly rather than refetching (unlike Diet Progress, which re-fetches per period since it deliberately doesn't pull the trainee's entire history at once).
- `lib/workoutFrequency.ts` (avg gap between session dates; avg rest days = gap − 1, the days off strictly *between* two training days) and `lib/workoutStreak.ts` (consecutive fully-completed weeks, Monday–Sunday, hitting `sessions_per_week` - walks backward from *last* week, deliberately excluding the current in-progress week from the streak count itself, shown separately as "this week so far" so an early-week visit doesn't read as a broken streak) are both scoped: frequency/rest-days respect the period selector, the streak and top movers below are always all-time (a streak or trend scoped to "this week" isn't meaningful).
- `lib/topMovers.ts::computeTopMovers` ranks every exercise logged at least twice by simple strength score (max working weight × avg working reps/set, same convention as `ExerciseHistoryChart`) at its *first* vs. *most recent* logged session - the plainest reading of "trended over time, simple score" per spec. Verified against the seeded history below (`Bent-Over Barbell Row` +108%, `Incline Dumbbell Press` −22%, etc. — both directions populate correctly).
- Selecting an exercise from Top Movers still opens the quick `ExerciseHistoryDialog` popup (same as Log). Below that, two collapsible cards were added per a later user request:
  - **`SessionHistoryCard.tsx`** - a small trigger card ("Last: Aug 28") that opens `SessionHistoryDialog.tsx`, a popup with its *own* independent period selector (same 2×2 pattern, deliberately not synced to the page's own period state so filtering history doesn't also change the frequency numbers above it) and a list of `SessionHistoryRow.tsx` entries - collapsed to one line (date, session label, exercise/set count, duration), expanding to every exercise logged that day with its sets, plus the trainee's own session notes. This is the "browse/edit past sessions" view the Log build deliberately deferred.
  - **`ExerciseHistoryCard.tsx`** - collapsible (default closed, per an explicit "I don't want them all open" request), and when opened shows a search-driven picker rather than always listing every logged exercise. Went through two rounds of feedback: first just "no full list, only search results as you type," then "actually show the full alphabetical list as soon as I click the field, like a dropdown" (i.e. a proper combobox, not a plain conditional list). Rebuilt on the same Radix `DropdownMenu` primitive `MultiSelectDropdown` uses (trigger button + portaled `DropdownMenuContent` holding a search `Input` and `DropdownMenuItem`s) after a plain absolutely-positioned `<div>` version got clipped by the card's own `overflow-hidden` - see the reusable lesson noted under "Diet → Food Bank filters + alternatives" above. Selecting an exercise renders `ExerciseHistoryContent.tsx` (the chart+list body extracted out of `ExerciseHistoryDialog` for exactly this kind of reuse) inline in the card, with a "← back" affordance to search again. This superseded and replaced an earlier, simpler `LoggedExerciseList.tsx` (deleted).
- `seed_workout_plan_sample`'s dev data (see above) was regenerated with a realistic Mon/Wed/Fri-style cadence across 5 weeks (rest days between sessions, one deliberately-missed week) rather than daily sessions, specifically so frequency/streak/movers would have something meaningful to show rather than a degenerate "trained every single day" case — not re-run via the command itself (which only seeds the *plan*, not sessions), but via a one-off throwaway script, per the existing convention of not committing session/log test data generators.

### Backend implementation notes (workout tab → Log — done)

- `WorkoutSession` gained `unique_together(trainee, plan_session, date)`, backing an upsert: re-logging the same session/day replaces it rather than duplicating (same convention as Diet's `LoggedMeal`).
- `WorkoutSessionSerializer` was rewritten as a nested read/write serializer (`logged_exercises` → `sets`, both many=True) — the frontend always POSTs/PUTs the complete current session, same pattern as `LoggedMealSerializer`. `validate()` checks every nested `plan_exercise` belongs to the `plan_session` being logged. `WorkoutPlanDetailSerializer` (nested `sessions → exercises`, `retrieve`-only) mirrors `DietPlanDetailSerializer`.
- `LoggedSetViewSet` gained an `exercise` query param filter (`GET logged-sets/?exercise=<id>`) — the one query that powers weight suggestions, PR detection, and the exercise-history chart, all computed client-side from this one flat, trainee-scoped fetch (no bespoke aggregation endpoint). `LoggedSetSerializer` gained read-only `session_date`/`exercise` fields so that flat fetch carries everything needed without extra joins.
- PR detection, weight suggestions, and the rest timer are deliberately not backend fields/endpoints — see "Frontend implementation notes" below.
- New committed `workouts/management/commands/seed_workout_plan_sample.py` (mirrors `seed_diet_plan_sample.py`) seeds a Push/Pull/Legs `WorkoutPlan` for `trainee1` from the Exercise bank, `sessions_per_week=3`.
- Verified via curl (throwaway, uncommitted, per existing convention): nested plan retrieval, session-log upsert (same POST payload twice → one row, not two), `?exercise=` history scoping (a trainer sees it read-only, another trainee's token sees nothing), 403 on trainer write.

### Frontend implementation notes (workout tab → Log — done, UI reworked after initial build per user feedback)

- `WorkoutLogPage.tsx` fetches the plan + the trainee's full session history once; `lib/workoutRotation.ts::nextSessionInRotation` picks the default session (the one after whichever was most recently logged, wrapping around) - purely client-side, no backend involvement, mirrors how Diet Progress computes date ranges client-side.
- `SessionLogForm.tsx` re-derives "already logged today" from the same already-fetched session list (no extra fetch) and pre-fills for editing if found - exactly how `LogMealSlot` already works for Diet.
- **Exercise cards are an accordion, not independent toggles.** `ExerciseLogBlock.tsx` cards default collapsed; only one can be expanded at a time, with the expanded-card id owned by `SessionLogForm` (not local `useState` inside each block) rather than each card tracking its own open/closed state. This was a deliberate fix, not the first design: with independent per-card state, reordering (below) could shuffle an *already-expanded* neighboring card into the exact spot a user had just tapped, which read as "the card I clicked just uncollapsed" - a real bug report, not a hypothetical.
- **Sets reveal one at a time within a card**, not all `target_sets` rows at once (also a rework - the first version showed every set's inputs simultaneously, which the user found overwhelming given "you can't do set 2 before set 1"). Each set is either a compact confirmed summary line (tap to reopen and edit) or, for at most one set at a time, a full editable row (weight/reps/RPE + a checkmark to confirm) - confirming one auto-reveals the next, up to `target_sets`, then an "+ Add set" button appears for extras. Warm-ups are a **separate section above working sets** with their own "+ Add warm-up set" button (not a per-row toggle mixed into the working-set rows, which was the original design) - same fill → collapse pattern, kept visually and structurally distinct.
- **Reordering** - up/down chevrons on each card (visible whether collapsed or expanded) - was added for the "someone's using the machine, I want to do a different exercise now" case. This is real, persisted ordering via the already-existing `LoggedExercise.order` field (from the Phase B backend build), not just a display trick: `SessionLogForm` tracks an `exerciseOrder: number[]` array (initialized from the plan's order, or from the existing log's saved order when editing), and Save sends each exercise's current position as its `order`.
- `lib/weightSuggestion.ts::suggestWeight` takes the *plan's own* `target_reps_min`/`target_reps_max` (a later fix - it originally used a hardcoded 8–12 for every exercise, which was silently wrong for anything with a different prescribed range, e.g. a 12–15-rep accessory move; verified against real seeded history that the fix changes the actual suggested status, not just the copy). `weightDirectionFeedback` adds a second, independent piece of feedback: rather than a static red/green tied to *which way* the suggestion points, each working-set weight input is tinted live against what's actually being typed - if the suggestion says lower and you type the same-or-higher weight it stays red with a "not lower than last time" note; type something lower and it turns green ("lowering the weight - that's what's suggested"); the reverse for a "raise" suggestion. Both live in `lib/weightSuggestion.ts`, consumed per-row in `ExerciseLogBlock`'s `SetEditorRow`.
- `lib/personalRecord.ts::checkPersonalRecord` (PR badge per confirmed working set, recomputed live) and `RestTimer.tsx` (local/ephemeral only - a Web Audio oscillator beep, no audio asset, one instance per exercise block seeded from `default_rest_seconds`) are unchanged from the initial build.
- `ExerciseHistoryDialog.tsx` + `ExerciseHistoryChart.tsx` (opened by clicking an exercise name while logging) are the one deliberate **dual-axis** chart in the app - unlike `ProgressTrendChart` (which avoided a dual axis by indexing to % of target), weight and avg-reps-per-set have no common base to index to, and the spec explicitly asks for two independent scales. Mitigated with axis-color-coding (each axis/line tinted to match) and direct end labels, same mark conventions otherwise. The chart+list body was later extracted into `ExerciseHistoryContent.tsx` so Progress's `ExerciseHistoryCard` could reuse it inline (see "workout tab → Progress" above) - the Dialog is now a thin wrapper around it.
- Trainer-authored `PlanSession.notes`/`PlanExercise.notes` (see the Core Features bullet above) render read-only as a "Note from your trainer" callout - the session note near the top of the form, each exercise's note inside its own expanded card above the weight-suggestion banner - so guidance shows up exactly where the trainee needs it while logging, not just on the separate read-only Plan tab (which also lets a trainer edit them inline).
- Scope cut, still deliberate: no "browse/edit past sessions" list *on the Log page itself* - Log only surfaces what's already logged for the currently-selected session+date. Session History (Progress tab) now covers that.

## Resolved Design Decisions

These were previously open questions, now settled:

- Workout plan structure: a rotation of sessions (Session 1, 2, 3, …), each with a fixed list of exercises/sets/target rep range, authored by the trainer. Sessions rotate rather than repeat on fixed calendar days — deliberately *not* the same model as the Diet plan's day-of-week `ReferenceMeal` (see below). Superseded an earlier "fixed weekly schedule of days" decision once the workout tab's actual spec arrived.
- Reps/weight granularity: logged per set, not per exercise.
- Diet plan scope: a reference meal plan (specific meals + ingredients + reference weights), not calorie/macro targets.
- Barcode/food data source: self-seeded reference table for Stage 1; Open Food Facts API integration planned for a later stage.
- Trainee accounts: trainees have their own logins and log their own data (not entered by the trainer on their behalf).
- Food log linkage: `FoodLog` can link to a `ReferenceMealItem` (planned adherence), a raw `FoodItem` (barcode/manual, not tied to a plan), or a `QuickLogItem` (private per-trainee shortcut) — exactly one of the three, enforced in the serializer and in `FoodLog.clean()`. Nutrients are computed once at save time and stored as a snapshot on the row, so later edits to a `FoodItem`/`QuickLogItem` don't retroactively change historical logs.
- Workout session linkage: `WorkoutSession` ties to a specific `PlanSession`, which pre-fills the prescribed template to log actuals against. Defaults to the next session in the rotation after the trainee's last logged one, but any session can be logged out of order.
- `QuickLogItem` is **private to its owner** for Stage 1 — no sharing/moderation. See **Deferred** below. (`FoodItem` got its own, separate private/public + approval model instead — see the Food Bank sharing decision below — but `QuickLogItem` itself is unchanged.)
- `day_of_week` (nullable, `null` = "applies every day") is used on `ReferenceMeal` (Diet) for weekday/weekend/fixed-daily schedules — no separate boolean (`workouts.DayOfWeek` choices, imported into `nutrition`). The Workout plan's `PlanSession` deliberately does **not** have this field: workout sessions rotate rather than repeat on fixed weekdays, so the two plan types now genuinely diverge instead of sharing one convention (this used to be identical on both; see the workout plan structure decision above).
- Exercise bank writes follow the same trust model as `FoodItem`: any trainer can add/edit (shared reference data, not trainee-scoped); all trainees get read access.
- Trainer connection (`connection` app): `QAThread`/`QAMessage` use plain `IsAuthenticated` + queryset scoping (both roles read/write within their own relationship) rather than an asymmetric permission class, since both parties post messages; `TrainerNote` stays asymmetric (trainer writes, trainee read-only + a dedicated `mark_read` action).
- Daily tracker (`tracker` app): `DailyMetric` is `unique_together(trainee, date)` — one row per trainee per day; `ActivityLog` allows multiple rows per day. The dashboard is a plain aggregation endpoint (`GET /api/tracker/dashboard/`), not a model.
- Frontend stack: TypeScript (not JS), Vite dev-server proxy to Django (not `django-cors-headers`), auth token in `localStorage` — confirmed with the user at the start of frontend work. Tailwind CSS v4 + shadcn/ui (Radix base, "Nova" preset) was later confirmed as the app-wide design system, restyling everything already built in the same pass rather than only new components.
- Food Bank sharing model: a trainee can add `FoodItem`s directly (not just via their private `QuickLogItem`s) — private items are visible only to their creator and need no approval; public items need approval from **any** trainer (not just the trainee's own trainer) before anyone else sees them, matching `FoodItem`'s existing "shared reference data" trust model (same as the exercise bank).
- Multi-ingredient ("composite") `FoodItem`s: an item can be `kind=composite`, built from other `FoodItem`s by weight (`FoodItemComponent`) instead of manually-entered macros; its per-100g nutrition is computed automatically from its components and recomputed whenever they change.
- Serving/measurement units (`serving_unit` + `serving_size_grams` on `FoodItem`) are an entry/display convenience only, not a new logging unit — a contributor can enter nutrition label values in whatever unit is convenient (e.g. "170g serving") and the app converts to per-100g at save time, but canonical storage and all downstream scaling math (logging, composites) remain per-100g/grams throughout the app.
- Reference meal plans support **options**: a `ReferenceMeal` (e.g. Breakfast) can offer several named `MealOption` alternatives (e.g. "Smoothie" vs. "Overnight Oats"), each with its own ingredients — a trainee picks one option per meal, not a combination of several.
- Structured meal logging (`LoggedMeal`): one log per trainee per meal slot per day (`unique_together(trainee, reference_meal, date)`) — re-logging a meal the same day replaces it rather than creating a second entry (no "second breakfast" support yet). Each log's `source` is `plan` (chosen option, weights freely editable from the reference) or `custom` (a deliberate whole-meal, off-plan swap built from the food bank) — never a mix of the two within one logged meal.
- Structured workout session logging (`WorkoutSession`): one log per trainee per plan session per day (`unique_together(trainee, plan_session, date)`) — re-logging the same session the same day replaces it, same convention as `LoggedMeal`. PR detection, weight suggestions, and the rest timer are all computed/handled client-side from already-fetched history, not stored fields — see "Backend/Frontend implementation notes (workout tab → Log...)".
- Food Bank substitution search: a fixed vocabulary of `MacroFilter`s (macro role) and `DietaryTag`s (diet-type compatibility), both hand/heuristic-classified for the existing seeded reference table since trainers/trainees pick them by hand only for items they add themselves going forward. Alternatives are computed **live at request time** from shared `macro_filters` + closeness in calories, not stored - unlike `Exercise.alternatives`, which *is* a stored, curated self-M2M, because Exercise Bank fetches its whole ~100-row list once (cheap to resolve alternatives client-side from what's already in memory) while Food Bank items are searched/paginated (no such in-memory list to resolve against).
- Workout Log's per-set entry is **one set at a time, not a flat form**: fill a set's weight/reps, confirm, it collapses to a summary line, the next set's inputs appear - reflecting that a trainee physically can't do set 2 before set 1 and that seeing every set's inputs simultaneously read as overwhelming. Warm-up sets are a separate section above working sets (their own "+ Add warm-up set" affordance), not a per-row toggle mixed into the working sets. Exercise cards are an accordion (only one open at a time, owned by the parent form, not each card) rather than independently-collapsible, specifically so reordering exercises can't shuffle an already-expanded neighbor into view and read as an accidental uncollapse. Exercise order within a logged session is trainee-editable (up/down, persisted via the existing `LoggedExercise.order`) for "someone's using that machine" swaps.
- Weight suggestions compare against the *plan's own* prescribed rep range (`PlanExercise.target_reps_min`/`max`), never a fixed 8–12 - and the weight-input color is **direction-following**, not status-static: it tracks whether the currently-typed weight actually follows the suggestion (lower than last time when told to lower, higher when told to raise), not just which direction the suggestion happens to point.

## Deferred (not built yet — don't forget)

- **Public/shared `QuickLogItem`s**: an `availability` (`private`/`public`) + `approval_status` (`pending`/`approved`/`rejected`) moderation workflow so a trainee's saved shortcut can be shared community-wide after admin approval. Skipped for Stage 1 (10 users, private-only is enough); revisit if trainees start asking to share shortcuts.
- **`ProgressPhoto`**: a dated progress-photo model (optionally linked to a `WorkoutSession`), feeding into the dashboard. Skipped for Stage 1; add alongside the dashboard endpoint when wanted.
- **Measurement-unit picker in the composite ingredient-builder's inline "add new ingredient" form**: currently grams/100g-only there; the full Measurement picker only lives in the main Add Food dialog. Add if it turns out to be a common friction point.
